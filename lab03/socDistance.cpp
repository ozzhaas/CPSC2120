#include <stdio.h>
#include <iostream>
#include <string>
#include <fstream>
#include "socDistance.h"


using namespace std;



int myhash(Node*** A, int table_size)
{
    unsigned int h = 0;
    for (unsigned int i = 0; i<b; i++)
        for (unsigned int j = 0; j < b; j++) {
            h = h * + (int)
        }
    }
    return h;
}


void allocate();

void deallocate();

void insert(Coords);

double find_distance(Point, Point);

double find_closest_cells(Node***);

double find_closest_coords(Point);



int main () {
















    return 0;
}
